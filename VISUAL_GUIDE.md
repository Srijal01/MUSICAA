# 📊 Visual Architecture Guide

## 🏗️ Old vs New Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                     OLD ARCHITECTURE (v1.0)                     │
└─────────────────────────────────────────────────────────────────┘

client/src/
├── components/          ← All components mixed together
│   ├── Navbar.jsx
│   ├── Footer.jsx
│   ├── ProductCard.jsx
│   ├── CartItem.jsx
│   └── ...
│
├── pages/               ← All pages in one place
│   ├── HomePage.jsx
│   ├── ProductsPage.jsx
│   ├── CartPage.jsx
│   └── ...
│
├── services/            ← API calls
└── utils/               ← Utilities

❌ Issues:
- Hard to find related code
- Components not reusable
- No clear patterns
- Difficult to scale


┌─────────────────────────────────────────────────────────────────┐
│                     NEW ARCHITECTURE (v2.0)                     │
└─────────────────────────────────────────────────────────────────┘

client/src/
├── features/            ← ✨ Feature modules (by domain)
│   ├── wishlist/
│   │   ├── WishlistContext.jsx
│   │   └── WishlistPage.jsx
│   ├── reviews/
│   │   └── ProductReviews.jsx
│   ├── products/
│   │   ├── CompareContext.jsx
│   │   └── CompareProductsPage.jsx
│   └── auth/
│
├── ui/                  ← ✨ Atomic Design System
│   ├── atoms/           (Building blocks)
│   │   ├── Button.jsx
│   │   ├── Input.jsx
│   │   ├── Badge.jsx
│   │   ├── Card.jsx
│   │   └── Rating.jsx
│   │
│   ├── molecules/       (Component combinations)
│   │   ├── Toast.jsx
│   │   ├── SearchBar.jsx
│   │   └── FilterPanel.jsx
│   │
│   ├── organisms/       (Complex sections)
│   └── templates/       (Page layouts)
│
├── shared/              ← ✨ Shared resources
│   ├── hooks/
│   │   ├── useLocalStorage.js
│   │   └── useDebounce.js
│   ├── contexts/
│   │   └── NotificationContext.jsx
│   └── constants/
│
├── components/          ← Legacy (to migrate)
├── pages/               ← Existing pages
├── services/            ← API calls
│   └── reviewService.js (✨ NEW)
└── utils/

✅ Benefits:
- Clear organization by feature
- Highly reusable components
- Easy to find code
- Scalable architecture
- Better developer experience
```

---

## 🎨 Atomic Design Hierarchy

```
┌────────────────────────────────────────────────────────────────────┐
│                     ATOMIC DESIGN SYSTEM                           │
└────────────────────────────────────────────────────────────────────┘

    ATOMS (Basic building blocks)
    ═══════════════════════════
    ┌─────┐  ┌─────┐  ┌─────┐  ┌─────┐  ┌─────┐
    │  🔘 │  │  📝 │  │  🏷️  │  │  📄 │  │  ⭐ │
    │ BTN │  │ INP │  │ BDG │  │ CRD │  │ RAT │
    └─────┘  └─────┘  └─────┘  └─────┘  └─────┘
    Button   Input    Badge    Card    Rating

              ↓ Combine atoms ↓

    MOLECULES (Simple combinations)
    ═══════════════════════════════
    ┌──────────────┐  ┌──────────────┐  ┌──────────────┐
    │   🔔         │  │  🔍  [____]  │  │  Filters     │
    │   Message    │  │  SearchBar   │  │  Panel       │
    │   [Dismiss]  │  │  Suggestions │  │  [Apply]     │
    └──────────────┘  └──────────────┘  └──────────────┘
    Toast             SearchBar          FilterPanel

              ↓ Combine molecules ↓

    ORGANISMS (Complex sections)
    ════════════════════════════
    ┌───────────────────────────────────────┐
    │  🎸 PRODUCT CARD                      │
    │  [Image]                              │
    │  ⭐⭐⭐⭐⭐ 4.5/5 (123 reviews)        │
    │  NPR 25,000                           │
    │  [Add to Cart] [Wishlist] [Compare]  │
    └───────────────────────────────────────┘

              ↓ Combine organisms ↓

    TEMPLATES (Page layouts)
    ════════════════════════
    ┌─────────────────────────────────────────┐
    │  NAVBAR                                 │
    ├──────────┬──────────────────────────────┤
    │ SIDEBAR  │  PRODUCT GRID                │
    │ Filters  │  [Card] [Card] [Card]       │
    │          │  [Card] [Card] [Card]       │
    │          │  [Card] [Card] [Card]       │
    ├──────────┴──────────────────────────────┤
    │  FOOTER                                 │
    └─────────────────────────────────────────┘
```

---

## 🔄 State Management Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                   CONTEXT-BASED STATE FLOW                      │
└─────────────────────────────────────────────────────────────────┘

App.jsx
  └── NotificationProvider ───┐
        └── WishlistProvider ──┼── Context Providers
              └── CompareProvider ─┘
                    └── Router
                          └── Routes
                                ├── HomePage
                                ├── ProductsPage
                                └── ProductDetailPage

Any component can access:
┌──────────────────────────────────────────────────────┐
│  const { success, error } = useNotification();      │
│  const { addToWishlist } = useWishlist();          │
│  const { addToCompare } = useCompare();            │
└──────────────────────────────────────────────────────┘

Benefits:
✅ No prop drilling
✅ Global state access
✅ Clean component code
✅ Easy to test
```

---

## 🌊 Data Flow Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                      DATA FLOW DIAGRAM                          │
└─────────────────────────────────────────────────────────────────┘

USER ACTION
    │
    ↓
┌─────────────┐
│ React       │ ← useNotification(), useWishlist()
│ Component   │
└──────┬──────┘
       │
       ↓
┌─────────────┐
│ Context     │ ← Global state management
│ API         │
└──────┬──────┘
       │
       ↓
┌─────────────┐
│ Service     │ ← API calls (axios)
│ Layer       │
└──────┬──────┘
       │
       ↓
┌─────────────┐
│ Backend     │ ← Express.js + MongoDB
│ API         │
└──────┬──────┘
       │
       ↓
┌─────────────┐
│ Database    │ ← MongoDB
│             │
└─────────────┘

Example: Adding to Wishlist
────────────────────────────
1. User clicks "Add to Wishlist" button
2. Component calls addToWishlist(product)
3. Context updates state & localStorage
4. Toast notification shows success
5. UI updates to show heart icon filled
```

---

## 📦 Feature Module Structure

```
┌─────────────────────────────────────────────────────────────────┐
│              FEATURE MODULE STRUCTURE (Example)                 │
└─────────────────────────────────────────────────────────────────┘

features/wishlist/
├── WishlistContext.jsx  ← State management
│   ├── WishlistProvider
│   ├── useWishlist hook
│   └── State: wishlist[], loading, etc.
│
└── WishlistPage.jsx     ← UI component
    ├── Uses: useWishlist()
    ├── Displays: Product cards
    └── Actions: Remove, Clear, Add to Cart

Usage anywhere in app:
┌────────────────────────────────────────┐
│ import { useWishlist } from           │
│   './features/wishlist/WishlistContext' │
│                                        │
│ const {                                │
│   wishlist,        // Array of items  │
│   addToWishlist,   // Function        │
│   removeFromWishlist, // Function     │
│   isInWishlist     // Function        │
│ } = useWishlist(); │
└────────────────────────────────────────┘
```

---

## 🎯 Component Reusability

```
┌─────────────────────────────────────────────────────────────────┐
│                   COMPONENT REUSABILITY                         │
└─────────────────────────────────────────────────────────────────┘

Button Component Usage Examples:
─────────────────────────────────

HomePage:
  <Button variant="primary">Shop Now</Button>

ProductCard:
  <Button variant="secondary" size="sm">View Details</Button>
  <Button variant="outline" icon="❤️">Wishlist</Button>

Cart:
  <Button variant="success" size="lg">Checkout</Button>
  <Button variant="danger">Remove</Button>

Admin:
  <Button variant="primary" loading>Saving...</Button>

One Component → Multiple Uses → Consistent UI
```

---

## 🔧 Custom Hooks Usage

```
┌─────────────────────────────────────────────────────────────────┐
│                     CUSTOM HOOKS FLOW                           │
└─────────────────────────────────────────────────────────────────┘

useDebounce Hook:
──────────────────
User types: "g" → "gu" → "gui" → "guit" → "guitar"
            ↓      ↓      ↓       ↓        ↓
Wait 500ms after each keystroke...
                                            ↓
                                    Search triggered
                                    only once!

Benefits:
✅ Reduces API calls
✅ Better performance
✅ Better UX


useLocalStorage Hook:
──────────────────────
Component → useLocalStorage('wishlist', [])
              ↓
        localStorage ←→ State
              ↓
    Persists across sessions

Benefits:
✅ Data persistence
✅ Automatic sync
✅ Easy to use
```

---

## 📱 Responsive Design Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                    RESPONSIVE DESIGN                            │
└─────────────────────────────────────────────────────────────────┘

Desktop (> 1024px)
┌─────────────────────────────────────────────────────────────────┐
│ NAVBAR                                                          │
├────────────┬────────────────────────────────────────────────────┤
│  Filters   │  [Card] [Card] [Card] [Card]                      │
│  Sidebar   │  [Card] [Card] [Card] [Card]                      │
│            │  [Card] [Card] [Card] [Card]                      │
└────────────┴────────────────────────────────────────────────────┘

Tablet (640px - 1024px)
┌──────────────────────────────────────┐
│ NAVBAR                               │
├──────────────────────────────────────┤
│ [Filter Button ▼]                    │
├──────────────────────────────────────┤
│ [Card] [Card] [Card]                 │
│ [Card] [Card] [Card]                 │
└──────────────────────────────────────┘

Mobile (< 640px)
┌────────────────────┐
│ NAVBAR             │
├────────────────────┤
│ [🔍] [🛒] [❤️]     │
├────────────────────┤
│ [Card]             │
│ [Card]             │
│ [Card]             │
└────────────────────┘
```

---

## 🎨 Animation Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                     ANIMATION TIMELINE                          │
└─────────────────────────────────────────────────────────────────┘

Button Hover:
─────────────
Normal → Hover → Active
 1.0    1.05     0.95    (scale)
        ↑
    200ms transition

Card Hover:
───────────
Normal → Hover
  0px     -4px    (translateY)
  0       Shadow   (box-shadow)
        ↑
    300ms transition

Toast Notification:
───────────────────
Off-screen → Slide-in → Stay → Fade-out
  100%        0%       3s      0%
   ↑          ↑              ↑
  Start    300ms          Duration
```

---

## 🔐 Authentication Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                   AUTHENTICATION FLOW                           │
└─────────────────────────────────────────────────────────────────┘

Login Process:
──────────────
1. User enters credentials
2. POST /api/auth/login
3. Backend validates
4. JWT token generated
5. Token saved to localStorage
6. Context updates user state
7. Redirect to dashboard

Protected Routes:
─────────────────
Route → ProtectedRoute → Check token → Allow/Deny
         ↓ If no token
     Redirect to /login

Review Submission:
──────────────────
User → Write Review → POST /api/reviews/:productId
                      ↓
                   Auth Middleware
                      ↓
                   Validate JWT
                      ↓
                   Save Review
```

---

## 📊 Performance Optimizations

```
┌─────────────────────────────────────────────────────────────────┐
│                  PERFORMANCE OPTIMIZATIONS                      │
└─────────────────────────────────────────────────────────────────┘

✅ Debounced Search
   User types → Wait 500ms → Single API call

✅ Lazy Loading
   Images load only when visible

✅ Context-based State
   No prop drilling = fewer re-renders

✅ localStorage Caching
   Wishlist/Compare cached locally

✅ Optimized Bundle
   Vite build optimization

✅ Code Splitting
   Routes loaded on-demand

Result: Fast, smooth user experience 🚀
```

---

## 🎉 Summary

```
┌─────────────────────────────────────────────────────────────────┐
│                    ARCHITECTURE BENEFITS                        │
└─────────────────────────────────────────────────────────────────┘

Old Way                    New Way
────────                   ────────
❌ Mixed components        ✅ Feature modules
❌ Hard to find code       ✅ Clear organization
❌ Not reusable            ✅ Atomic components
❌ Prop drilling           ✅ Context API
❌ Basic features          ✅ Advanced features
❌ Difficult to scale      ✅ Highly scalable

Result: Professional, maintainable, feature-rich application! 🎯
```
